{
    "version": 2,
    "builds":[{
        "src":"/sendText",
        "use": "@vercel/node"
    }],
    "routes":[
        {
            "src": "/sendText",
            "dest": "/"
        }
    ]
}
